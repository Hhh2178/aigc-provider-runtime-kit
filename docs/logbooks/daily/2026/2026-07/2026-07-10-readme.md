# 2026-07-10 README

## Intent

Rewrite the public README as a complete entrypoint for users evaluating, installing, integrating, and contributing to the 0.2.0 unified provider runtime.

## Changed

| Area | Files | Purpose |
| --- | --- | --- |
| Public documentation | `README.md` | Lead with unified runtime value, provide end-to-end examples, explain contracts and boundaries, and document development/release workflows |

## Verification

| Check | Result |
| --- | --- |
| `npm run harness:verify:project` | Passed |
| `npm run type-check` | Passed |
| `npm test` | Passed (26 tests) |
| `npm run test:package` | Passed |

## Decisions

- Keep the public README in English for broader open-source reuse.
- Make the unified runtime the primary onboarding path while retaining low-level client documentation.
- Use only public APIs that ship in version 0.2.0.

## Risks

- Provider-specific video endpoints still require adapter configuration and result normalization.

## Next

Keep README examples synchronized with public contracts as new adapters are added.
