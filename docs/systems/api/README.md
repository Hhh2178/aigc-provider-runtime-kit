# API Integration System

This project does not ship a hosted API in v0.1.

Host applications should provide:

- secret storage
- provider persistence
- authentication and permissions
- task creation
- audit logs

This kit provides reusable contracts and runtime helpers only.
