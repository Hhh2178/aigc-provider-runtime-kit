# DEVLOG.md

Detailed development records live in `docs/logbooks/`.

Use:

- `docs/logbooks/daily/` for daily implementation and governance records.
- `docs/logbooks/releases/` for release records.
- `docs/logbooks/validations/` for validation reports.
- `docs/logbooks/incidents/` for incident analysis.

This file is a compatibility entry only and should not become the primary diary.
